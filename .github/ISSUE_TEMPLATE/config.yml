# GitHub adds its own "Report a security vulnerability" entry to the chooser
# whenever SECURITY.md exists, so no contact_link is declared here — a second
# one duplicates it under the same name. SECURITY.md carries the detail and the
# private-reporting route.
blank_issues_enabled: false
